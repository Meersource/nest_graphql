type Book {
id: ID!,
title: String!,
price: Int
}

type Query {
    books: [Book]
}

# // 1 schema likhna ha
# schema k againt query likhni ha 
# phir query k againt resolver likhna ha k query k against kis trha ka data get ho ga
# ab jo ap ny resolver bnaya ha is ko as a provider register krna ha jis module ma ap ny jis module k ander resover bnaya ha usi module k ander is ko as provider regisrter kry gy
# phir BookMudule ko app Module ma registered krna ha